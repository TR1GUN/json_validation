{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Настройки SNTP-серверов",
  "required": [ "Settings" ],
  "properties": {
    "Settings": {
      "type": "array",
      "description": "Массив настроек",
      "items": {
        "type": "object",
        "description": "Настройки SNTP-сервера",
        "required": [ "addr" ],
        "properties": {
          "addr": {
            "type": "string",
            "description": "Адрес",
            "examples": [ "ntp1.stratum2.ru" ]
          }
        }
      }
    }
  }
}
